# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                           @controllers.ShortenerController.index

GET     /shorten/:url               @controllers.ShortenerController.shorten(url:String)

GET     /:key                       @controllers.ShortenerController.redirect(key:String)

